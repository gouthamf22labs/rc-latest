diff --git a/node_modules/@whiskeysockets/baileys/lib/Socket/chats.js b/node_modules/@whiskeysockets/baileys/lib/Socket/chats.js
index 9a7947f..0063450 100644
--- a/node_modules/@whiskeysockets/baileys/lib/Socket/chats.js
+++ b/node_modules/@whiskeysockets/baileys/lib/Socket/chats.js
@@ -58,7 +58,7 @@ export const makeChatsSocket = (config) => {
     };
     const fetchPrivacySettings = async (force = false) => {
         if (!privacySettings || force) {
-            const { content } = await query({
+            const result = await query({
                 tag: 'iq',
                 attrs: {
                     xmlns: 'privacy',
@@ -67,7 +67,7 @@ export const makeChatsSocket = (config) => {
                 },
                 content: [{ tag: 'privacy', attrs: {} }]
             });
-            privacySettings = reduceBinaryNodeToDictionary(content?.[0], 'category');
+            privacySettings = reduceBinaryNodeToDictionary(result?.content?.[0], 'category');
         }
         return privacySettings;
     };
diff --git a/node_modules/@whiskeysockets/baileys/lib/Socket/groups.js b/node_modules/@whiskeysockets/baileys/lib/Socket/groups.js
index df19870..c940516 100644
--- a/node_modules/@whiskeysockets/baileys/lib/Socket/groups.js
+++ b/node_modules/@whiskeysockets/baileys/lib/Socket/groups.js
@@ -43,6 +43,7 @@ export const makeGroupsSocket = (config) => {
         const groupsChild = getBinaryNodeChild(result, 'groups');
         if (groupsChild) {
             const groups = getBinaryNodeChildren(groupsChild, 'group');
+            let extracted = 0;
             for (const groupNode of groups) {
                 const meta = extractGroupMetadata({
                     tag: 'result',
@@ -50,6 +51,11 @@ export const makeGroupsSocket = (config) => {
                     content: [groupNode]
                 });
                 data[meta.id] = meta;
+                // rc-latest: accounts in 1,000+ groups made this loop block the event loop for
+                // seconds. Yield every 50 groups so other work runs in between; data is unchanged.
+                if (++extracted % 50 === 0) {
+                    await new Promise((resolve) => setImmediate(resolve));
+                }
             }
         }
         // TODO: properly parse LID / PN DATA
@@ -61,8 +67,13 @@ export const makeGroupsSocket = (config) => {
         if (attrs.type !== 'groups') {
             return;
         }
-        await groupFetchAllParticipating();
-        await sock.cleanDirtyBits('groups');
+        try {
+            await groupFetchAllParticipating();
+            await sock.cleanDirtyBits('groups');
+        } catch (err) {
+            // groupFetchAllParticipating throws when a deleted/stale group is in the
+            // socket state; swallow here so the WS handler does not crash the socket
+        }
     });
     return {
         ...sock,
diff --git a/node_modules/@whiskeysockets/baileys/lib/Socket/messages-send.js b/node_modules/@whiskeysockets/baileys/lib/Socket/messages-send.js
index d61e064..f52106f 100644
--- a/node_modules/@whiskeysockets/baileys/lib/Socket/messages-send.js
+++ b/node_modules/@whiskeysockets/baileys/lib/Socket/messages-send.js
@@ -49,6 +49,18 @@ export const makeMessagesSocket = (config) => {
                     content: [{ tag: 'media_conn', attrs: {} }]
                 });
                 const mediaConnNode = getBinaryNodeChild(result, 'media_conn');
+                // query() returns undefined when the iq times out (socket.js swallows the
+                // timeout Boom and returns undefined), and its assertNodeErrorFree guard is
+                // skipped for a falsy result. Without this check we'd deref undefined and
+                // throw an opaque "Cannot read properties of undefined (reading 'attrs')"
+                // instead of reporting the real cause: the socket is dead/unresponsive.
+                if (!mediaConnNode) {
+                    // Drop the rejected promise from the cache. It is only cleared on socket
+                    // end otherwise, so one timeout would fail every later media send on this
+                    // socket with the same error.
+                    mediaConn = undefined;
+                    throw new Boom('Failed to fetch media upload connection: WhatsApp did not return a media_conn node (socket disconnected or query timed out)', { statusCode: 503, data: result });
+                }
                 // TODO: explore full length of data that whatsapp provides
                 const node = {
                     hosts: getBinaryNodeChildren(mediaConnNode, 'host').map(({ attrs }) => ({
@@ -478,12 +490,27 @@ export const makeMessagesSocket = (config) => {
                     attrs: {},
                     content: bytes
                 });
+                // Forward additionalNodes (e.g. <meta polltype="creation"> for poll
+                // creation, <meta event_type="creation"> for events) so newsletter
+                // polls register server-side for voting. Without this, channel poll
+                // votes are rejected with "your vote couldn't be saved".
+                if (additionalNodes && additionalNodes.length > 0) {
+                    binaryNodeContent.push(...additionalNodes);
+                }
+                // Poll/event stanzas must be typed 'poll'/'event' so the server sets
+                // them up for voting/responses. Media and text stay 'text' (newsletter
+                // media is sent via the plaintext path and relies on type 'text').
+                const newsletterMsgType = getMessageType(message);
+                const newsletterStanzaType =
+                    newsletterMsgType === 'poll' || newsletterMsgType === 'event'
+                        ? newsletterMsgType
+                        : 'text';
                 const stanza = {
                     tag: 'message',
                     attrs: {
                         to: jid,
                         id: msgId,
-                        type: getMessageType(message),
+                        type: newsletterStanzaType,
                         ...(additionalAttributes || {})
                     },
                     content: binaryNodeContent
