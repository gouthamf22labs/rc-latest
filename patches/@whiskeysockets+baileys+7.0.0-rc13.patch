diff --git a/node_modules/@whiskeysockets/baileys/lib/Socket/messages-send.js b/node_modules/@whiskeysockets/baileys/lib/Socket/messages-send.js
index abc1234..def5678 100644
--- a/node_modules/@whiskeysockets/baileys/lib/Socket/messages-send.js
+++ b/node_modules/@whiskeysockets/baileys/lib/Socket/messages-send.js
@@ -484,7 +484,8 @@ export const makeMessagesSocket = (config) => {
                     attrs: {
                         to: jid,
                         id: msgId,
                         type: getMessageType(message),
-                        ...(additionalAttributes || {})
+                        ...(additionalAttributes || {}),
+                        ...extraAttrs
                     },
                     content: binaryNodeContent
diff --git a/node_modules/@whiskeysockets/baileys/lib/Socket/chats.js b/node_modules/@whiskeysockets/baileys/lib/Socket/chats.js
index 9a7947f..0063450 100644
--- a/node_modules/@whiskeysockets/baileys/lib/Socket/chats.js
+++ b/node_modules/@whiskeysockets/baileys/lib/Socket/chats.js
@@ -58,7 +58,7 @@ export const makeChatsSocket = (config) => {
     };
     const fetchPrivacySettings = async (force = false) => {
         if (!privacySettings || force) {
-            const { content } = await query({
+            const result = await query({
                 tag: 'iq',
                 attrs: {
                     xmlns: 'privacy',
@@ -67,7 +67,7 @@ export const makeChatsSocket = (config) => {
                 },
                 content: [{ tag: 'privacy', attrs: {} }]
             });
-            privacySettings = reduceBinaryNodeToDictionary(content?.[0], 'category');
+            privacySettings = reduceBinaryNodeToDictionary(result?.content?.[0], 'category');
         }
         return privacySettings;
     };
diff --git a/node_modules/@whiskeysockets/baileys/lib/Socket/groups.js b/node_modules/@whiskeysockets/baileys/lib/Socket/groups.js
index df19870..992e0fd 100644
--- a/node_modules/@whiskeysockets/baileys/lib/Socket/groups.js
+++ b/node_modules/@whiskeysockets/baileys/lib/Socket/groups.js
@@ -61,8 +61,13 @@ export const makeGroupsSocket = (config) => {
         if (attrs.type !== 'groups') {
             return;
         }
-        await groupFetchAllParticipating();
-        await sock.cleanDirtyBits('groups');
+        try {
+            await groupFetchAllParticipating();
+            await sock.cleanDirtyBits('groups');
+        } catch (err) {
+            // groupFetchAllParticipating throws when a deleted/stale group is in the
+            // socket state; swallow here so the WS handler does not crash the socket
+        }
     });
     return {
         ...sock,
